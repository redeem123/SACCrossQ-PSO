function focalNet = createFocalNetwork(numFeatures, hiddenNeurons)
    % Initialize Focal Neural Network exactly as in paper
    focalNet = initializeFocalNetwork(numFeatures, hiddenNeurons);
end

